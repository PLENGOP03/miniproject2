<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>Dashboard</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.3.1/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
      <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Lato">
      <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
      <link rel='stylesheet' href='/stylesheets/style.css' />
</head>
<style>
  
  /* body,p,h1,h2,h3,h4,h5,h6 {font-family: "Lato", sans-serif;}  */
    body {
    background-image: url('../images/pexels-lumn-167698.jpg');
    background-repeat: no-repeat;
    background-attachment: fixed;  
    background-size: auto;
    height: 100%;
    color: #fd8905;
    line-height: 1.8;
}
</style>

<div class="w3-top">
  <div class="w3-bar" id="myNavbar">
    
      
   
    <a href="/admin/productForm" class="w3-bar-item w3-button" >หน้าสินค้า</a>
    
    
  </div>
  

</div><br><br>
  
<body>
  <div class="container">
    <div class="row justify-content-center mt-5">
      <div class="col-10 col-md-6">
        <div class="card">
          <div class="card-header bg-primary text-light">
            <h5 class="card-title mb-0">Dashboard</h5>
          </div>
          <div class="card-body">
            <div class="row">
              <div class="col-md-4 mb-4">
                <h6 class="card-subtitle mb-2 text-muted">ยอดขายของวันนี้</h6>
                <% if (product.length > 0) { %>
                  <p> <% let total = product[0].TotalSales %> </p>
                  <h2 id="total-project" class="card-title"><%= total %></h2>
                <% } else { %>
                  <p>No payments found.</p>
                <% } %>
              </div>

              <div class="col-md-4 mb-4">
                <h6 class="card-subtitle mb-2 text-muted">Total record</h6>
                  <h2 id="total-record" class="card-title">x</h2>
              </div>

              <div class="col-md-4 mb-4">
                <h6 class="card-subtitle mb-2 text-muted">Total budget</h6>
                <h2 class="card-title">x</h2>
              </div>
            </div>

            <div class="row">
              <div class="col-md-4 mb-4">
                <h6 class="card-subtitle mb-2 text-muted">Total cost</h6>
                <h2 class="card-title">x</h2>
              </div>

              <div class="col-md-4 mb-4">
                <h6 class="card-subtitle mb-2 text-muted">Total profit</h6>
                <h2 class="card-title">x</h2>
              </div>
              <div class="col-md-4"></div> <!-- เพิ่ม div เปล่าๆ เพื่อให้ตำแหน่งเท่ากัน -->
            </div>
            <hr>

            <canvas id="myChart"></canvas>
          </div>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
  <style>
  .card-body .row {
    display: flex;
    flex-wrap: wrap;
    margin-right: -15px;
    margin-left: -15px;
  }
  .card-body .col-md-4 {
    flex: 0 0 calc(33.33% - 30px);
    margin-right: 15px;
    margin-left: 15px;
  }
  </style>
  <canvas id="myChart" width="400" height="400"></canvas>
<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<script>
  var total = document.getElementById('total-project').textContent;
  var t2 = document.getElementById('total-record').textContent;
  var t1 = Math.round( total );
  var t2 = t1/2;
  var t3 = t1/3;
  var t4 = t1/6;
  var t5 = t1/7;
  var ctx = document.getElementById('myChart').getContext('2d');
  var myChart = new Chart(ctx, {
    type: 'bar',
    data: {
      labels: ['Total project', 'Total record', 'Total budget', 'Total cost', 'Total profit'],
      datasets: [{
        label: 'counts',
        data: [t1 , t2, t3, t4, t5], // ตรวจสอบว่าไม่มี comma ที่ไม่ต้องการ
        backgroundColor: [
          '#2ecc71',
          '#3498db',
          '#9b59b6',
          '#f1c40f',
          '#e74c3c'
        ]
      }]
    },
    options: {
      responsive: true,
      maintainAspectRatio: true,
      scales: {
        yAxes: [{
          ticks: {
            beginAtZero: true
          }
        }]
      }
    }
  });
</script>

